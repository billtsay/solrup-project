<?xml version="1.0" encoding="UTF-8"?>
<features name="webapp-${project.version}" xmlns="http://karaf.apache.org/xmlns/features/v1.4.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.4.0">

    <feature name="market" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/market/${project.version}/war?Web-ContextPath=/market</bundle>
    </feature>

    <feature name="canvas" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/canvas/${project.version}/war?Web-ContextPath=/canvas</bundle>
    </feature>

    <feature name="grocery" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/grocery/${project.version}/war?Web-ContextPath=/grocery</bundle>
    </feature>

    <feature name="login" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/login/${project.version}/war?Web-ContextPath=/login</bundle>
    </feature>

    <feature name="directory" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/directory/${project.version}/war?Web-ContextPath=/directory</bundle>
    </feature>

    <feature name="home" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/home/${project.version}/war?Web-ContextPath=/home</bundle>
    </feature>

    <feature name="lhome" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:com.solrup.webapp/lhome/${project.version}/war?Web-ContextPath=/lhome</bundle>
    </feature>

    <feature name="jack" version="${project.version}">
        <feature prerequisite="true">war</feature>
        <bundle>webbundle:mvn:org.apache.jackrabbit/jackrabbit-webapp/2.17.6/war?Web-ContextPath=jack</bundle>
    </feature>

</features>
